namespace Models.Enums
{
    /// <summary>
    /// Enum that contains all the existent roles in the web app.
    /// </summary>
    public enum Roles
    {
        Administrator = 1,
        User
    }
}